const initialState = {
    data: [],
    isLoading: true,
    error: null
};

export const trendReducer = (state = initialState, action) => {

    switch (action.type) {

        default:
            return state;
    }

};